# 项目开发协作规则

本文件用于保存用户为 Lab-AI-Assistant 设定的长期开发规则，适用于本项目。开始任务前阅读本文件；用户在当前对话中的明确要求优先于本文件中的默认约定，但仍须遵守系统和平台约束。

## 需求讨论与实施边界

- 用户提出功能想法、询问可行性、要求理解代码或提供设计方案时，先阅读相关代码并解释或给出方案，不直接修改业务代码。
- 每次依据设计方案开始修改或实施之前，必须先向用户说明本次方案及修改范围，取得用户明确确认后再执行。提出功能需求或要求设计方案本身不等于确认执行。
- 确认仅适用于本次已确认的方案和范围；执行中若需要修改方案或扩大范围，必须再次取得用户确认后执行。已确认范围内的常规步骤无需逐步重复确认。
- 用户要求维护本规则文件时，可以直接更新本文件，不视为业务代码实施。

## 执行失败反馈

- 执行过程中出现失败时，及时告知用户失败的步骤、原因和影响，以及拟采取的下一步；不得隐瞒失败或声称已经完成。
- 原因尚未查明时明确说明，区分已确认的事实和待验证的推测；不能编造原因。
- 后续处理仍须遵守已确认的方案范围；涉及方案变更时先取得用户确认。

## Git push 确认

- 每次执行 Git push 前，必须告知用户目标远程仓库、分支及待推送内容，并取得针对本次 push 的明确确认后再执行。
- 设计方案确认、代码修改授权、commit 授权或此前的 push 确认，均不视为本次 push 的确认。
- 推送失败后，先说明原因；再次执行 push 前重新取得确认。使用其他工具进行等价远程推送也遵守本规则。

## 规则维护

- 用户说“以后都这样做”“把这条写进规则”等，或明确提出新增、修改、删除开发规则时，及时更新本文件，并简要告知变更内容。
- 只将用户明确表达的长期要求固化为规则；不要把助手自己的建议或一次性任务要求自动升级为长期约束。
- 新规则替代旧规则时更新对应条目，避免保留互相矛盾的要求。
- 本文件保持简洁；设计方案、执行日志和临时任务进度放在各自文档或对话中，不堆积到本文件。

## 后续规则

代码风格、测试要求及其他开发约定，待用户提出后补充。
