# Phase 1 MVP 直接依赖（安装成功后用 uv pip freeze 生成 requirements.lock 锁全量版本）
fastapi
uvicorn[standard]
pydantic-settings
python-multipart

# 文档解析（unstructured 的 pptx/xlsx 解析依赖在 extras 中）
pymupdf==1.28.2
python-docx
unstructured[pptx,xlsx]

# 向量库
chromadb==1.5.9

# LLM
dashscope

# 存储（SQLite 或 MySQL + pymysql）
sqlalchemy
pymysql

# 测试
pytest
jieba==0.42.1          # 混合检索关键词分词（Phase 3-06）
rank-bm25==0.2.2       # 混合检索 BM25（Phase 3-06）

# 用户认证与数据库迁移（Phase 4-02）
pwdlib[argon2]
PyJWT[crypto]
email-validator
alembic
